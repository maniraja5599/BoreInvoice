import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anjaneyaborewells.invoice',
  appName: 'Borewell Invoice',
  webDir: 'dist'
};

export default config;
